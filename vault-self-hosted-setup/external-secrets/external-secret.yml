apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: myapp-all-secrets
  namespace: example
spec:
  refreshInterval: 5s

  secretStoreRef:
    name: vault-backend
    kind: SecretStore

  target:
    name: myapp-all-secrets

  dataFrom:
    - extract:
        key: myapp